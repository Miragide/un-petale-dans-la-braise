# “Un pétale dans la braise”

French novel written during the NaNoWriMo 2018 edition.

## Compile the book

### Archlinux

Install `pandoc` and `texlive-most`.

```
sudo pacman -S pandoc texlive-most
```

Then:

```
make
```
